# 开放问题看板（自动生成，禁止手改）

> 本文件由 GitHub Actions 在 issue 或评论变动时自动重建（.github/workflows/issues-board.yml）。
> 讨论请进对应 issue；这里只是快照，方便 git pull 后本地阅读。流程规则见 CLAUDE.md。
> 用法：每次开工先 git pull，先看「按人待办」里自己登录名下的条目；issue 全文与评论用 `gh issue view N --comments` 读。
> 当前开放 2 条，近 14 天关闭 0 条。

## 按人待办（每次开工先看自己登录名下这一节）

收录口径：被指派的、正文或评论中被 @ 的、自己提出的开放 issue。

### @relativequntum（史静宇 · 项目总管）

- #2 [裁决] 确定模块划分与 src/ 目录结构 【你已回应，等对方】 https://github.com/relativequntum/robot-arm/issues/2
- #1 [任务] 全员配置 gh CLI、pull.rebase 并登记 GitHub 登录名 【你已回应，等对方】 https://github.com/relativequntum/robot-arm/issues/1

> 成员A / 成员B / 成员C 登录名 `<待登记>`：名单齐后由总管用 `gh api users/<login>` 核实并回填本 workflow 的 MEMBERS 数组（同时回填 CLAUDE.md 对照表与 docs/协作者交接文本.md）。回填前这些成员不会出现在本节，请直接看下方模块章节。

## 公共

#### #2 [裁决] 确定模块划分与 src/ 目录结构
- 链接：https://github.com/relativequntum/robot-arm/issues/2
- 指派：@relativequntum（史静宇） ｜ 标签：公共、裁决 ｜ 评论：0 ｜ 更新：2026-09-15 16:05（北京时间）

> **提出方**：@relativequntum（史静宇，项目总管）
> **属主（拍板人）**：@relativequntum（史静宇）
> **相关方**：全体协作者（欢迎附议）
> 
> **背景与问题**：
> 仓库初始化时决定**暂不划分模块**，`CLAUDE.md` 文件归属表只填了总管一行，`src/` 目录结构与模块标签（当前只有 公共/硬件/文档）都待定。名单齐后需要确定：按什么维度分工、每人归属哪些目录/文档、公共契约层放哪里。
> 
> **候选方案**：
> - A. 按功能层：驱动通信层（motorbridge/CAN）/ 运动学与轨迹 / 视觉感知 / 上层应用与示教 / 仿真与 URDF / 公共契约。每人认领一层。
> - B. 按端到端功能：每人认领一个功能（如视觉抓取、示教回放、遥操作录制、仿真联调），公共层由总管持有。
> - C. 混合：先按 A 划出公共契约层与驱动层归总管，其余按 B 认领。
> 
> **裁决结论（属主关闭前填写，并回填 `docs/开放问题结论台账.md`；同时更新 `CLAUDE.md` 归属表、`src/` 骨架与看板 workflow 的模块标签）**：

#### #1 [任务] 全员配置 gh CLI、pull.rebase 并登记 GitHub 登录名
- 链接：https://github.com/relativequntum/robot-arm/issues/1
- 指派：@relativequntum（史静宇） ｜ 标签：公共、任务 ｜ 评论：0 ｜ 更新：2026-09-15 16:05（北京时间）

> **下发方**：@relativequntum（史静宇，项目总管）
> **被指派者**：全体协作者（加入仓库后由总管追加 Assignee）
> 
> **任务书（可整段交给你的 Claude Code 执行，登录一步留给本人）**：
> 
> 1. 安装 gh CLI：Windows `winget install --id GitHub.cli -e`；macOS `brew install gh`。装完**新开终端**，本人执行 `gh auth login --web` 用自己的 GitHub 账号授权（在 Claude Code 会话内可用 `! gh auth login --web`），`gh auth status` 显示自己账号即成功。
> 2. 执行 `git config --global pull.rebase true`（看板机器人会向 main 自动提交，推送被拒时 `git pull --rebase` 再推即可）。
> 3. 在本 issue 回帖登记：`姓名｜GitHub 登录名｜意向方向（驱动/运动学/视觉/应用/仿真/文档，可多选或写"待定"）`。
> 4. 读一遍仓库根目录 `CLAUDE.md` 与 `docs/协作者交接文本.md`，有看不懂或不同意的地方直接在本 issue 回帖。
> 
> **完成通报要求**：每位被指派者完成 1~4 后回帖"已完成"（可让 Claude 直贴）。全员回帖后由下发方核实登录名（`gh api users/<登录名>`）、回填 `CLAUDE.md` 对照表与看板 workflow 的 MEMBERS/NAMES，然后验收关闭。本 issue 属任务类，关闭后**不回填**结论台账。


---

最后同步：2026-09-15 16:05（北京时间）
