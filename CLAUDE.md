# CLAUDE.md — 项目协作宪法（所有成员的 Claude Code 会话必须严格遵守）

> 生效日期 2026-09-15。本文件被每位成员的 Claude Code 自动加载，规则写在这里＝下发给全员的 Claude。

本项目：**Seeed reBot DevArm B601-RS 机械臂的功能开发**。多人协作、单仓库（https://github.com/relativequntum/robot-arm ，private，单 main 分支），每人在本地用 Claude Code 干活，协作全部走 GitHub Issue。

## 必读文档导航

| 你要做什么 | 先读什么 |
|---|---|
| 了解项目全貌与仓库结构 | `README.md` |
| 查硬件官方资料（BOM、URDF、SDK、wiki 链接） | `docs/硬件/官方文档索引.md` |
| 查自己名下的待办 | `docs/看板.md` —— 开放问题看板（机器人自动生成，**禁止手改**） |
| 查已关闭问题的结论 | `docs/开放问题结论台账.md` |
| 新成员接入 | `docs/协作者交接文本.md` |
| 想知道协作模式为什么这么设计 | `gh-issue协同开发模式.md`（跨项目方法论模板，原文保留；示例标签与项目背景来自他项目，不适用本项目；凡与本文件冲突处**一律以本文件为准**） |

## 模型使用规范

- **默认用 Opus**。日常开发、写代码、写测试、常规分析、读文档都用 Opus。
- **复杂问题升级 Fable 5**：复杂架构设计、疑难联调（CAN / 运动学 / 训练链路）、关键结论校验、多步长链路推理时，用 `/model` 切到 Fable 5，用完切回。
- 派发子代理同理：默认 opus，确需更强能力才用 fable，并说明理由。

## Git 协作规则（红线）

- 项目**只用单 main 分支**，不建长期分支。
- **开始任何工作前先 `git pull`**。
- **完成一个小功能就 commit 一次**，提交信息用中文写清楚改了什么。
- **每次工作结束必须 `git push`**。
- **禁止** `git push --force`；**禁止**提交数据文件（数据集、录像、模型权重、大 STEP/mesh）与任何密钥（token、API key、`.env`）。
- **首次接入执行 `git config --global pull.rebase true`**。推送被拒时先 `git pull --rebase` 再推——看板机器人会向 main 自动提交，这类被拒属正常，不必发群里。
- **遇到内容冲突或 rebase 报错，不要自行强解**，把完整报错原样发到群里。
- commit 前用 `git status` 检查暂存内容，确认没混入数据文件或密钥。
- commit 信息里**别随手写 `closes #N`**——那会真的关掉 issue；只在确实要自动关联关闭时才写。

## Issue 流程

**issue 是唯一存档地，看板是拉取式触达，本文件是行为宪法，gh CLI 是 Claude 的手。**

### 三种 issue 类型

| 类型（标题前缀） | 用途 | 谁表态 | 关闭条件 | 关闭后 |
|---|---|---|---|---|
| `[会签]` | 多方对齐口径 / 接口 | 全部会签方 | 达成一致，**提出方**关闭 | 一句话结论回填 `docs/开放问题结论台账.md` |
| `[裁决]` | 契约变更 / 纪律解释（模块划分、公共层、配置 schema） | 相关方可附议，**总管（唯一属主）拍板**（属主＝项目总管 `relativequntum`） | 总管填结论并完成契约修改后关闭 | 一句话结论回填台账 |
| `[任务]` | 总管向多人下发执行事项 | 每位被指派者回帖**完成通报**（做不了 / 需延期也算有效回帖） | 全员回帖后**下发方验收关闭** | **不回填台账**（台账不是打卡记录） |

- 模板在 `.github/ISSUE_TEMPLATE/`：`huiqian.md` / `caijue.md` / `renwu.md`；`config.yml` 已关闭空白 issue，**必须用模板开**（网页端自动套模板；gh CLI 走 `--body-file` 把模板正文填好再传，见下文「开 issue 的标准命令」）。
- 开 issue 必做三件事：打**模块标签**（`公共` / `硬件` / `文档`）+ **类型标签**（`会签` / `裁决` / `任务`），并在 Assignees 指派相关成员。
- 讨论全程走 issue 线程；**结论必须落在 issue 里**。群里聊出的共识，由提出方搬回 issue 评论区——看板只同步 issue，群聊不进存档。
- **任务下发一律走 `[任务]` issue**，不靠群里转发或口头交代——群里说的任务不进看板、无人跟踪，与「写在文档里的会签请求不算发出」同理；正文按 `renwu.md` 写成可直接交给对方 Claude 执行的任务书。
- 六个标签（`公共` / `硬件` / `文档` / `会签` / `裁决` / `任务`）已建好，由看板 workflow 幂等维护；新增或改名标签时改 workflow 里的 `labels` 数组，push 后到 Actions 页手动触发一次 `issues-board-sync`。
- 台账编号从 `Q-01` 起接续，由提出方（会签）或总管（唯一属主，裁决）回填。

### 看板 `docs/看板.md`

- 由 `.github/workflows/issues-board.yml` 在 issue / 评论任何变动后自动重建并提交回 main，**任何人不得手改**（手改会被下一次重建覆盖）。
- 顶部「按人待办」按登录名分节，收录三类：被指派 / 正文或评论被 @ / 自己提出。每条带状态标注：

| 状态标注 | 含义 |
|---|---|
| 【待你首次回应】 | 这条 issue 你一次言都没发过，球在你脚下 |
| 【有新动态待你看，最后发言 @xx】 | 你回过了，但之后别人又说了话，需要你再看一眼 |
| 【你已回应，等对方】 | 球不在你这儿，等别人 |

- **宽口径 ≠ 欠账**：按人待办收的是「与你相关」的全部条目，真正欠你动手的看状态标注。汇报时必须区分「与我相关 N 条」和「待我动手 M 条」，否则数字打架。
- **看板是快照**：读到的「最后发言人」以重建时点为准，别把三天前的发言当成刚刚发生。
- 看板超过一天没更新时，去仓库 Actions 页看 `issues-board-sync` 是否失败（常见原因：Settings → Actions → General 的 Workflow permissions 不是 Read and write，或 main 上的分支保护挡住了机器人推送），**不要手改看板**。

### 通知分级

普通事项**不群 @**——靠「对方下次开工 pull 看板」这个节拍传达即可。**只有三种情况必须群 @**：① 阻塞他人开工；② 有截止硬约束（如「上机测试前必须闭环」）；③ 需当日回应。

### @ 的书写规范

- 一律写 `@登录名（姓名）`，例如 `@relativequntum（史静宇）`。@ 登录名保证 GitHub 通知送达与 issue 内定位，括注姓名让所有人一眼看出是谁的待办。
- **必须显式 @**：凡在 issue 正文或评论里请**任何协作者**做事——备案、知会、确认、验证、回填、改契约文件等——都必须按上述格式显式 @ 到人。只写「请总管备案」「请硬件同学确认」而不 @，GitHub 不通知、看板按人待办也不收录，**视为未送达**，对方不响应责任在提出方。
- **写在文档里的会签请求不算发出**。文档里长出来的请求清单必须逐条开成 issue 才算数，否则就是死角，没人跟踪。

### gh CLI —— 全员标准配置（装一次，Claude Code 就能自动读写 issue）

- 安装：Windows `winget install --id GitHub.cli -e`；macOS `brew install gh`；Ubuntu 见 GitHub CLI 官方安装说明。
- 装完执行 `gh auth login --web --hostname github.com --git-protocol https`：它是交互式的，会打印一次性 code 并要你按 Enter 打开浏览器，用自己的 GitHub 账号授权（只需一次，顺带把 git 凭据配好，这正是之后能 clone private 仓库的原因）。**登录由本人操作**，两种方式任选：在系统终端（Windows 开 PowerShell / Windows Terminal）执行；或在 Claude Code 输入框里以 `!` 前缀运行同一条命令（`! gh auth login --web --hostname github.com --git-protocol https`，`!` 前缀会把命令交给本会话终端交互执行，输出直接落在会话里）。之后让 Claude 跑 `gh auth status` 验证，显示自己账号即成功。
- 装好后你的 Claude Code 就能：`gh issue list`、`gh issue view N --comments` 读全文与讨论；`gh issue comment N` 代你回评；（提出方）`gh issue close N` 关闭。看板是离线总览，gh 负责实时细节与回写。
- **开 issue 的标准命令**（`config.yml` 的 `blank_issues_enabled: false` 只管网页端，gh CLI 会绕过模板强制，所以正文与标签必须自己带全）。**注意 `--template` 只在有 TTY 的交互模式下生效，Claude Code 的 Bash 是非交互 shell，带 `--template` 必报 `must provide --title and --body when not running interactively`，而 `--template` 又与 `--body-file` 互斥——所以 Claude 一律走 `--body-file`**：

  ```
  # Claude 的做法：读 .github/ISSUE_TEMPLATE/huiqian.md，删掉文件顶部 --- 之间的 YAML front matter，
  # 按栏位（提出方 / 会签方 / 模块标签 / 背景与问题 / 建议方案 / 结论待填）填好内容写入临时文件，再：
  gh issue create --title "[会签] 一句话概括问题" --body-file <填好的临时文件> --label 会签 --label 硬件 --assignee <登录名>
  ```

  裁决用 `caijue.md` + `--label 裁决`，任务用 `renwu.md` + `--label 任务`；本人想用网页模板时才跑 `gh issue create --web`（会打开浏览器，不适合让 Claude 跑）。**Claude 代开 issue 时四要素缺一不可：正文按对应模板栏位写全（缺栏位视为未发出）、类型标签、模块标签、`--assignee`**（没标签会掉进看板「未打模块标签」兜底节，没指派对方收不到通知）。临时文件写在系统临时目录（`tempfile.gettempdir()`，Windows 下别用 `/tmp`），不要留在仓库里。

### 被 @ / 被指派后怎么响应

1. 打开 Claude Code（项目目录），对它说（模板，替换 `#N`）：

   > 我在 issue #N 被指派了会签。先 git pull 看 `docs/看板.md` 我名下待办，再用 gh 读 #N 全文和评论，结合相关文档给出意见草稿，我确认后你用 gh issue comment 贴出。

2. **表态类评论**（同意 / 反对 / 口径主张）**必须经你本人过目定稿**再让 Claude 贴；**完成通报类**评论（按已达成结论改完了什么、commit 号）Claude 可以直接贴，不必等你。
3. Claude 代贴评论时正文必须是**可直接上墙的定稿**，不得残留「回复草稿」「定稿后贴到…」之类的抬头或内部说明（此类事故在同模式的其他项目发生过）。
4. 关闭与回填分类型：
   - **（会签）** 达成一致后由**提出方**关闭 issue，并把一句话结论回填 `docs/开放问题结论台账.md`；
   - **（裁决）** 由**总管**填结论、改完契约文件后关闭，并回填台账；
   - **（任务）** 全员回帖后由**下发方验收关闭**，**不回填台账**。

   结论若要求改文档 / 代码，各自在归属文件内修改、commit、push。
5. **总管纪律**：裁决类 issue 集中在总管（唯一属主）身上，总管养成「当日清欠」习惯，并定期让 Claude 盘点球权分布（谁在等谁）。

## 角色、姓名与 GitHub 登录名对照

开 issue 指派 Assignee、正文 @ 时**一律用登录名**（git 提交作者名 ≠ GitHub 登录名，不要拿提交作者名当 @ 对象）。

| 角色 | 姓名 | GitHub 登录名 |
|---|---|---|
| 项目总管 | 史静宇 | `relativequntum` |
| 成员A | 待登记 | `<待登记>` |
| 成员B | 待登记 | `<待登记>` |
| 成员C | 待登记 | `<待登记>` |

- 名单齐后由总管用 `gh api users/<login>` 逐个核实（返回 200 且 `name` / `login` 对得上才算数），再回填**三处**：本表、`docs/协作者交接文本.md` 的成员信息表，以及 `.github/workflows/issues-board.yml` 里的 `MEMBERS` 数组（追加一行 `['登录名', '姓名 · 角色', '姓名']`）。**漏改 workflow，该成员在看板「按人待办」里不会有章节，等于收不到任何待办**（不报错、看板照常生成）。回填后手动触发一次 `workflow_dispatch` 验证他那一节出现。
- 成员登记是**总管**的动作（`.github/` 归总管）。**未经核实不得直接 @**，防止 @ 到 GitHub 上的同名陌生人。

## 文件归属

**模块划分与 `src/` 目录结构尚未确定**，由首条 `[裁决]` issue 拍板后回填本表。在此之前，除总管行外一律「待分工后填写」。

| 角色 | 负责范围 | 归属文件 / 目录 |
|---|---|---|
| 项目总管（`relativequntum`） | 仓库治理、协作基建、硬件资料索引、成员登记 | 根目录治理文件（`CLAUDE.md`、`README.md`、`gh-issue协同开发模式.md`、`.gitignore`、`.gitattributes`、`.env.example`）、`.github/`（含 ISSUE_TEMPLATE 与看板 workflow，成员登记须改这里的 `MEMBERS`）、`docs/`、`share-data/` 的目录说明、公共契约文件 |
| 成员A | 待分工后填写 | 待分工后填写 |
| 成员B | 待分工后填写 | 待分工后填写 |
| 成员C | 待分工后填写 | 待分工后填写 |

- **模块划分定稿前的行为边界**（未分工期间全员适用）：不得新建 `src/` 下的目录、不得修改他人文件或公共文件；看板「按人待办」里自己名下没有条目时**停下来等总管指派**，不要自行开工改代码；确有要做的事，先开 `[任务]` / `[会签]` issue。
- **全员可写的回填位（归属红线的唯一例外）**：`docs/开放问题结论台账.md` 与 `docs/硬件/官方文档索引.md` 第七节是**全员可写**的结论回填位，按角色分工回填（会签＝提出方，裁决＝总管，硬件结论＝求证的提出方）。回填以外的改动（改表头、删条目、重排结构）仍归总管。除这两处外，`docs/` 一律只有总管可改。
- `docs/看板.md` 由机器人生成，**不属于任何人，谁都不许手改**。
- **公共契约文件**（分工后的 `src/common/`、配置 schema、CAN / 关节参数等全项目共用定义）**任何人不得擅自修改**；确需变更，开 `[裁决]` issue，总管拍板后统一改并通知全员。

## 技术栈与代码规范

- **主语言 Python 3.10+**：官方 `motorbridge` SDK（`pip install motorbridge`）、`reBotArm_control_py`（Pinocchio 正逆运动学 + 重力补偿）、Seeed 的 LeRobot fork（`Seeed-Projects/lerobot`）。
- **ROS 2（Humble / Jazzy）为可选路线**，不是默认路径；是否启用由 `[裁决]` issue 决定。
- **硬件事实**（仅列官方文档明确写出的）：6 DOF + 1 夹爪；DC 48V 供电；总线 CAN，比特率 **1 Mbps**，Linux 下走 **socketcan**，默认通道 **can0**。关节-电机型号映射、CAN ID 数值、关节限位与扭矩参数官方文档未给出，**不要臆测**，需要就开 `[会签]` issue（打 `硬件` 模块标签）求证，结论回填 `docs/硬件/官方文档索引.md` 第七节并登记进 `docs/开放问题结论台账.md`。
- **运行环境**：硬件控制只在 Linux（Ubuntu / Jetson）上经 CAN 运行；**Windows 只做开发与文档**，要跑硬件必须到 Linux 机器上。Jetson 平台必须装 PCAN 驱动，缺失会导致通信持续失败。
- 代码规范：所有文件（代码、文档、配置）一律 **UTF-8**；Windows 下不要用 `/tmp` 路径（用 `tempfile.gettempdir()` 或项目内临时目录）；避免向控制台打印非 ASCII 导致 GBK 乱码（输出写文件）；密钥全部放 `.env`（已 gitignore），仓库只留 `.env.example`。

### 硬件操作安全（上机前必读，来自官方文档）

- **保持 1 米安全距离**；上电或使能控制器前确认工作区内无人、无障碍物。
- **禁止热插拔电机**：插拔电机前必须先断电源。
- **断电 / 通信中断会失控**（官方明确警告 runaway），任何运动场景都要同等注意。
- **实机前先仿真**：跑任何实机 demo 前先在 RViz 验证规划路径，并随时准备停掉控制器；每条运动指令都要仔细复核，严禁危险操作。
- **环境**：禁止在潮湿、高温或多尘环境中使用。

> 以上是不可协商的安全红线。**完整的安全与工程注意事项**（标定顺序、装配扭矩、线束卡扣、加工精度、BOM 与 URDF 命名等会随官方文档更新的条目）见 `docs/硬件/官方文档索引.md` 第六节，**上机前必读**；两处有出入时以官方文档索引为准（本文件只管红线）。

## 数据目录规范

| 目录 | 进 git？ | 放什么 |
|---|---|---|
| `share-data/` | ✅ 进 | 小体量样例、配置文件、标定结果等需要全队共享的内容 |
| `local-data/` | ❌ 不进 | LeRobot 数据集、录像、模型权重、运行日志、大 STEP / mesh 文件 |

大文件一律走 `local-data/`；需要别人也能拿到的大件，在 issue 里给出获取方式，不要往仓库里塞。

- `share-data/` 已在仓库里落地（见 `share-data/README.md`），直接往里放，**不要另建 `shared-data/` / `share_data/` 之类的变体**。
- `local-data/` 由使用者首次用到时自建，已在 `.gitignore` 里整目录忽略，不预置。

## 每次会话的标准工作流

1. `git pull`；
2. 打开 `docs/看板.md`，先看「按人待办」里**自己登录名**下的条目，按状态标注逐条响应；
3. 干活：只在自己归属的文件内工作；先设计后编码；改公共契约先开 `[裁决]` issue；
4. 小步 commit，中文提交信息写清楚改了什么；
5. 会话结束 `git push`，需要群里同步的按「通知分级」判断要不要 @。
