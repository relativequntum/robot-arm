# gh + Issue 驱动的多人协同开发模式（可移植模板）

> 沉淀自 2026-07 研究生金融科技大赛项目（5 人 7 角色、单仓库、各自用 Claude Code 独立开发）的实战。
> 用法：新项目启动时把本文发给项目管理者的 Claude Code，让它按此模式搭建协作基建；或按文末落地清单逐步实施。
> 适用前提：多人分模块开发、单 GitHub 仓库、每人本地用 Claude Code 作为开发助手。

## 一、模式总览（一张图）

```
成员开工 → git pull ──┬→ CLAUDE.md（宪法，会话自动加载）
                      └→ docs/看板.md「按人待办」→ 领到自己名下 issue
                              ↑ GitHub Actions 机器人：issue 一变动就重建看板、提交回 main
成员响应 → Claude 用 gh 读 issue 全文 → 起草意见/干活 → gh 回帖 → 提出方关闭
```

核心思想：**issue 是唯一存档地，看板是拉取式触达，CLAUDE.md 是行为宪法，gh CLI 是 Claude 的手**。人只做两件事：拍板表态、真急事时群里 @ 一声。

## 二、六个组件

### 1. CLAUDE.md 宪法（仓库根目录）

每个成员的 Claude Code 会话自动加载它，规则写进去=下发给全员的 Claude。必备章节：文件归属表（每人只改自己的目录/表/文档，契约文件唯一属主）、Git 红线（单 main 分支、先 pull、小步中文 commit、结束必 push、禁 force push）、Issue 流程、每次会话标准工作流（pull → 看板按人待办 → 干活 → push）。

### 2. gh CLI 全员标配

- 安装：Windows `winget install --id GitHub.cli -e`；Mac `brew install gh`。装完新开终端 `gh auth login --web` 用自己账号浏览器授权一次，`gh auth status` 验证。
- 授权后成员的 Claude 即可：`gh issue list / view N --comments / comment N / close N`——自动读被 @ 的问题全文、代贴回复、关闭 issue。
- 让成员自助配置的技巧：把安装指令写成"粘贴给你的 Claude Code"的一段话，登录一步留给本人（`!` 前缀在会话内运行交互命令）。

### 3. Issue 三类型（各配模板 + 标签）

| 类型 | 用途 | 谁表态 | 关闭条件 | 关闭后 |
|---|---|---|---|---|
| [会签] | 多方对齐口径/接口 | 全部会签方 | 达成一致，**提出方**关闭 | 一句话结论回填台账文档 |
| [裁决] | 契约变更/纪律解释（schema、公共层） | 相关方可附议，**唯一属主拍板** | 属主填结论+完成契约修改后关闭 | 结论回填台账 |
| [任务] | 管理者向多人下发执行事项 | 每位被指派者回帖**完成通报** | 全员回帖后**下发方验收关闭** | 不回填台账（不是打卡记录） |

配套：模块标签（M1~M6/公共/数据…）+ 类型标签（会签/裁决/任务）；`.github/ISSUE_TEMPLATE/` 放两三个模板，模板里预置"提出方/会签方/建议方案/结论（关闭前必填）"栏位。

### 4. 看板机器人（GitHub Actions，拉取式自动触达的关键）

`.github/workflows/issues-board.yml`：监听 issues + issue_comment 全部事件 → 用 actions/github-script 重建 `docs/看板.md` → 提交回 main。要点：

- **按人待办章节放最顶**：每个成员一节，收录三类（被指派 / 正文或评论被 @ / 自己提出），每条标状态——【待你首次回应】（你没发过言）/【有新动态待你看，最后发言 @xx】（你回过但别人又说了）/【你已回应，等对方】。状态由"谁最后发言"推出，天然是欠帖花名册。
- 之后按模块标签分组列 issue 全文摘录（正文截断 ~1200 字，全文点链接），近 14 天已关闭单独一节。
- 工程细节：labels 幂等创建（422 忽略）；commit 回写用 `git pull --rebase` 重试环防并发冲突；concurrency 取消旧任务；用 GITHUB_TOKEN 即可（私有仓库也行）；显示名带真实姓名（`@登录名（姓名）`）。
- 效果：**成员 pull 仓库 = 收到全部待办**，无需任何客户端配置；gh 负责实时细节与回写，看板负责离线总览。

### 5. 响应流程（写进 CLAUDE.md）

被指派/被 @ 后：对自己的 Claude 说模板句（"我在 issue #N 被指派了会签。先 git pull 看看板我名下待办，再用 gh 读 #N 全文和评论，结合我的模块文档给出意见草稿，我确认后你用 gh issue comment 贴出"）。纪律：**表态类评论必须本人过目定稿**；**完成通报类**（按已达成结论改完了什么+commit 号）Claude 可直贴；**结论必须落在 issue**——群聊内容不进存档，聊出的共识由提出方搬回 issue。

### 6. 通知分级（避免群消息轰炸）

普通事项**不群 @**——靠"对方下次开工 pull"这个节拍传达；只有三种情况必须群 @：阻塞他人开工、有截止硬约束（如"评测前必须闭环"）、需当日回应。@ 写法一律 `@登录名（姓名）`，配一张"角色-姓名-GitHub 登录名"对照表进 CLAUDE.md（**用 API 核实登录名**，防止 @ 到同名陌生人；git 提交作者名≠登录名）。

## 三、配套机制（按需取用）

- **`git config --global pull.rebase true` 全员**：机器人会向 main 提交，推送被拒是常态，rebase 后重推即可；只有真冲突才上报。
- **showcase 成果展示页**：每模块一个演示命令，跑共享样例数据产出"输入→中间判定→输出"的人话版报告，挑 5~10 条对照放进仓库共享目录——开发期全队互见进展的窗口。
- **数据需求清单模式**：需要外部数据 → 在清单文件登记一行 → 数据专员按清单取数并回填状态/路径；数据文件不进 git，清单进 git。
- **任务下发不靠群转发**：管理者开 [任务] issue 指派多人，正文写成可直接交给 Claude 执行的任务书；成员 Claude 开工看板领任务。
- **交接文本模式**：新成员接入=建空文件夹+粘贴一段启动文本（clone、必读清单、文件归属、当前任务、纪律），文本集中放一个文档每人一段。

## 四、实战教训（血泪版）

1. **写在文档里的会签请求 = 死角**。请求不开成 issue 就不进看板、没人跟踪——"文档里长出来的请求清单"必须逐条转 issue 才算发出。
2. 看板是**宽口径**（相关面），实际欠账看状态标注——汇报时要区分"与我相关 17 条"和"待我动手 4 条"，否则数字打架。
3. 看板是快照：读到的"最后发言人"以重建时点为准，别把三天前的发言当"刚刚"。
4. Claude 代贴评论时，**别把草稿抬头一起贴上去**（"# 回复草稿……定稿后贴到……"曾原样上墙）。
5. commit 信息里别随手写 `closes #N`——真会关掉 issue；只在确实要自动关联关闭时用。
6. 管理者自己是最大瓶颈：裁决类 issue 集中在唯一属主身上，要养成"当日清欠"习惯，或让 Claude 定期盘点球权分布（谁欠谁）。

## 五、落地清单（新项目 8 步）

1. 建仓库，写 CLAUDE.md（归属表、Git 红线、Issue 流程、工作流、@对照表）；
2. `.github/ISSUE_TEMPLATE/` 放会签/裁决（/任务）模板；
3. 写看板 Actions workflow（参考本模式第 4 组件要点；原版实现见大赛项目仓库 `.github/workflows/issues-board.yml`）；
4. 建标签体系（模块+类型，workflow 里幂等创建即可）；
5. 全员发"让 Claude 装 gh"的粘贴文本，各自授权；
6. 核实并登记全员 GitHub 登录名（API 查证），写入对照表；
7. 全员 `git config --global pull.rebase true`；
8. 首批问题按三类型开 issue，跑通"提出→看板→响应→关闭→回填"一个完整循环（第一条 issue 建成时看板自动生成，即验证链路）。

> 原版完整实现（workflow YAML、模板、CLAUDE.md 全文）在仓库 https://github.com/relativequntum/UESTC-finance-competition ，可直接抄。
